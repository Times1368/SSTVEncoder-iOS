# Claude 协作入口

先完整阅读 [PROJECT_HANDOFF.zh-CN.md](PROJECT_HANDOFF.zh-CN.md)，再检查当前分支、工作区和用户本轮范围。
其中有项目进度、真实接口、设计目标、测试证据、已取消的旧约束和共享文件边界。

- App 当前代码基点为 1.1.3（5），不是已完成商业验收的产品；图库与设置仍为骨架。
- 交接说明建议的首个独立任务是图库存储和图库页，具体范围仍以用户给你的请求为准。
- 不因本文件自动开展所有待办。先说明修改范围和测试，再按 TDD 完成约定的一块。
- UI 工作不改 SSTVKit/DSP、麦克风 tap、编码基线，不新引第三方运行库或 SwiftData。
- 快照已有 completedRows；中途片段 progress 为 nil。保留未知信息，不能伪造整图进度。
- 独立工作目录、独立功能分支；同一目录只有一个 Git 写入者。共享文件先确认所有者。
- Windows 不运行 Swift/Xcode；本机命令非交互且明确超时不超过 120 秒。
- 推送后给 Actions 链接，不等待；CI 通过后由唯一集成者合入 main，不强推、不删他人分支。
- 旧设计/勘察文档仅作历史参考。与用户最新确认或交接说明冲突时，先报告，不照旧实施。
